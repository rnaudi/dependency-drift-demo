[versions]
kotlin_version = "2.1.0"
spring_boot_version = "4.0.0"
jackson_version = "3.0.0"

# External dependencies
guava_version = "[34.0.0, 35.0)"
slf4j_version = "2.0.+"
opentelemetry_version = "1.40.0"

# Test dependencies
junit_version = "5.11.0"
mockito_version = "5.12.0"

# Plugins and processors
spotless_version = "7.0.0"
